# Copiar a `.env` y rellenar. `.env` está en .gitignore: no se versiona.
#
# Clave de 21st.dev para el servidor MCP declarado en `.mcp.json`, que la referencia
# como ${API_KEY_21ST} y nunca guarda el valor.
API_KEY_21ST=
